info:
  name: Get filled data
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/forms/latest
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
